/**
 * Contains the interfaces and classes used for implementing the different CLI commands,
 * as well as the {@link project.star_wars_universe.cli.commands.CommandsList} class which keeps track of them all.
 */
package project.star_wars_universe.cli.commands;